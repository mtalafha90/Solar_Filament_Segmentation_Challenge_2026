# Core numerical and image-processing stack
numpy>=1.24
scipy>=1.10
scikit-image>=0.21
scikit-learn>=1.3
Pillow>=10.0
PyYAML>=6.0

# Reading GONG FITS observations
astropy>=5.3

# Deep learning. Install the build that matches your CUDA version; see
# https://pytorch.org/get-started/locally/
torch>=2.0

# Required: the competition submission format is pycocotools compressed RLE,
# and some COCO exports use it for annotations too
pycocotools>=2.0.7

# Development
pytest>=7.4
